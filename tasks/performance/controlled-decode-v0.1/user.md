The fixed input below exists only to create a repeatable prefill workload. Read
the entire payload before following the output instruction.

--- FIXED INPUT START ---
{source_text}
--- FIXED INPUT END ---

Output the lowercase word `vector` repeatedly, separated by single spaces.
Do not number the words, add punctuation, explain the task, or stop voluntarily.
The runtime will stop generation at the configured token limit. Begin now with
`vector`.
