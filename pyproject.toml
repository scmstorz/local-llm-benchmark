[project]
name = "local-llm-benchmark"
version = "0.1.0"
description = "Personal, reproducible benchmark lab for local Ollama models"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = []

[project.scripts]
local-llm-benchmark = "local_llm_benchmark.cli:main"

[tool.setuptools]
packages = ["local_llm_benchmark"]
