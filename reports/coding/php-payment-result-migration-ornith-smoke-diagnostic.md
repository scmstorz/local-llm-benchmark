# PHP payment migration: Ornith smoke transport diagnostic

This is a post-hoc, zero-inference diagnostic. It does not replace or amend the
excluded source run.

## Source outcome

| Field | Value |
| --- | --- |
| Case | `coding.dev.php-payment-result-migration` v1.0.0 |
| Run | `coding-run-20260902T072710Z-423acd55` |
| Deployment | `ornith-1.5:35b` |
| Response SHA-256 | `69a58dd052cce4f273c9830725eedeaf97fe013fc8c25febf0d4c85d07649a30` |
| Original output status | `malformed` |
| Original failure | `malformed_artifact` |
| Original verified success | `false` |
| Original candidate tests | Not executed |

The response used two ordinary Markdown `php` code fences. The case required
path-bearing `<<<FILE path="...">>>` envelopes. The controller correctly
refused to infer installation paths during the source run.

## Diagnostic transformation

After the outcome was known, the user authorized a separate semantic check.
The diagnostic applied one explicit rule:

1. require exactly two `php` code blocks;
2. map the unique `CheckoutService` class to `src/CheckoutService.php`;
3. map the unique `RetryPolicy` class to `src/RetryPolicy.php`;
4. remove only Markdown fence lines and add only the required envelope lines;
5. do not change candidate PHP code.

Code-byte identity was verified before execution:

| Path | Source code SHA-256 | Normalized code SHA-256 | Equal |
| --- | --- | --- | --- |
| `src/CheckoutService.php` | `74baf893a089d3f2f5ad4252b8fbd4ee699f37c6c35e1ad0fdb8bf9ca9584466` | `74baf893a089d3f2f5ad4252b8fbd4ee699f37c6c35e1ad0fdb8bf9ca9584466` | yes |
| `src/RetryPolicy.php` | `d64d1396e1ad7380e8aa3cd50061db14aecf557a8c5eecf1dfef925792be8a08` | `d64d1396e1ad7380e8aa3cd50061db14aecf557a8c5eecf1dfef925792be8a08` | yes |

## Diagnostic result

The normalized artifact installed within scope under PHP 8.4.17.

| Verification layer | Result |
| --- | ---: |
| Artifact installation | pass |
| Scope integrity | pass |
| Public checks | 4/4 |
| Hidden checks | 8/8 |
| Semantic outcome under normalized transport | verified success |

No new model request, retry, code repair or test feedback occurred.

## Interpretation

The immutable source-run outcome remains a strict output-contract failure. The
diagnostic establishes a different fact: the exact PHP code generated by
Ornith passes every current deterministic behavior check when delivered
through a valid artifact envelope.

The supported conclusion is therefore:

> Ornith demonstrated verifier-confirmed code semantics but failed strict
> automated artifact delivery on this attempt.

The diagnostic was defined after the source outcome became known. It is not a
formal comparison attempt, cannot estimate reliability and must not be
promoted to `verified_success` for the original run.
